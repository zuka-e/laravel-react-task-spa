import React from 'react';

import { List } from '@material-ui/core';

const BoardMenuList: React.FC = () => (
  <List component='nav' aria-label='board-menu'>
    {''}
  </List>
);

export default BoardMenuList;
